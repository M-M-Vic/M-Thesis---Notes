# Thesis Review & Action Plan — 2026-06-12

> Full audit via `/thesis-review`. Read-only diagnostic pass; no files edited.
> Build: clean rebuild of commit `00b27b0` (`latexmk -pdf main.tex`).

## A. Executive Summary

The thesis is in **excellent structural health**. The build is clean (0 errors,
0 LaTeX/hyperref warnings, no undefined refs or duplicate labels), now **105 pages**
(up from 99pp at the 2026-06-11 G1 audit, reflecting the recent Model-X rewrite and
the expanded B/B2/C2/comparison/results/conclusion sections). Notation discipline
(`π₀` vs `π(0,0)`, in-queue `N1/N2/N`, named principles, stability statements) holds
across every chapter checked — no conflations found anywhere. Three previously-open
`[AUTHOR]`/scaffolding items appear to have been **quietly resolved** during the
recent Model-X rewrite and results cleanup (not yet recorded in
`docs/REVIEW_CHECKLIST.md`). New findings are minor: a couple of structural-heading
inconsistencies between the B2/B21/C21 sections and the C2 template (ordering and
heading scaffolding — both are **author judgement calls**, not bugs), a handful of
new hardcoded loss-fraction numbers in the comparison chapter that extend an
already-known macro-coverage gap, and a slightly larger but still cosmetic overfull-box
count (20 vs 17). Four author-only items from the prior audit remain open and are
carried forward unchanged.

## B. Findings by Category

### B.1 Build cleanliness
- **PASS.** Exit 0, `main.pdf` = 105 pages, 1.65 MB. Zero `Warning` lines in
  `main.log` — no undefined refs/citations, no multiply-defined labels, no
  hyperref duplicate-destination warnings (the G1 fix holds).
- 297 `\label`s defined, 285 unique `\ref`/`\eqref` targets, all resolve; **no
  duplicate labels**.
- Overfull `\hbox`: **20** (was 17 at G1); Underfull: **4** (was 5). All cosmetic.
  Worst offenders:
  | width | location |
  |---|---|
  | 61.97 pt | `chapters/13_results.tex:27-44` (unchanged worst offender from G1) |
  | 57.85 pt | `chapters/06_model_a.tex:208` (was line 209 — shifted by 1, unchanged) |
  | 43.37 pt | `chapters/09_model_c2.tex:265` (**new** — C2 grew by 147 lines) |
  | 41.57 pt | `chapters/09_model_c2.tex:74` (**new**) |
  | 36.86 pt | `chapters/01_introduction.tex:41-59` (**new**; the old 48.15pt box in `02_literature.tex` from G1 is now gone — fixed by the literature-review rewrite) |
  | <15 pt ×15 | new small boxes scattered in `09_model_c2.tex`, `10_model_b21.tex`, `11_model_c21.tex`, `13_results.tex`, `14_conclusion.tex` |

### B.2 Notation & convention audit (CLAUDE.md §1–§3)
- `π₀` vs `π(0,0)`: **no conflations found** in B, X, B2, B21, C2, C21, comparison,
  or results — every Corollary distinguishes them explicitly (e.g.
  `chapters/12_comparison.tex:300` states `π(0,0)=ρ·π₀` correctly).
- `N1, N2, N` consistently in-queue (service-customer excluded) wherever discussed.
- Named principles — PASTA, memorylessness, Poisson superposition, Pollaczek–
  Khinchine, Little's Law — are explicitly named everywhere they're used,
  including where PK's *inapplicability* is the point (B, X, B2, B21, C2, C21 all
  name it explicitly when explaining why no probabilistic route exists).
- Stability stated explicitly and correctly everywhere: B (`ρ<1`), X (positive
  recurrence, argued via unbounded drift + Erlang-A analogy, `chapters/05_model_x.tex:23-33`),
  B2 (`ρ<1`), B21 (`ρ<1` + `ρ1` remark, `eq:B21:stability`), C2
  (`λ2·E[B_C]<1`, `eq:C2:stability`), C21 (own condition `eq:C21:stability` with an
  explicit explanation of why `ρ<1` is not required).

### B.3 Section-structure conformance (CLAUDE.md §6)
- **C2** (`chapters/09_model_c2.tex`, the template): fully conformant, no
  regressions from the recent 147-line expansion.
- **B, X** (`chapters/07_model_b.tex`, `chapters/05_model_x.tex`): both are flat
  prose with no `\subsection`/`\subsubsection` scaffolding at all. Content order
  matches the spine implicitly (preamble → stability → Corollary(π₀,π(0,0)) →
  Volterra/kernel reduction → synthesis/limits paragraph), and the "open" framing
  is well-handled in prose (`07_model_b.tex:3` "intentionally left open, in
  structural parallel with Model-X"; `05_model_x.tex:563-594` explicit synthesis
  back to A/B/B2/C2). But neither uses C2's heading-based "keep the heading, explain
  why the route doesn't apply" convention — there are simply no headings.
- **B2** (`chapters/08_model_b2.tex`): Theorem+Corollary (step 4) is correctly
  structured (`thm:B2` line 34, `cor:B2:pi00` line 72), and the reduced fundamental
  equation (step 3) has its own subsection (`ssec:B2:reduction`, line 18). However,
  steps 5–7 (Determination of `P_y(y)`, Recovery of `P(x,y)`, Limits & sanity
  checks) are embedded as unlabelled "Stage 2"/"Stage 3" prose inside the proof of
  Theorem 1 (lines 200, 358) rather than standalone subsections, and there is no
  "Probabilistic approach" heading kept-but-explained (the inapplicability of PK is
  explained only inline at line 204). Remark 1 (`rem:B2:singular_limit`, γ1→0+) is
  present and well-integrated (lines 387-429).
- **B21** (`chapters/10_model_b21.tex`), B2's sibling: by contrast **does** have
  full standalone subsections, including an explicit `\subsection{Limits and
  sanity checks}` (line 305) — so B2 and B21 now diverge in how thoroughly the
  spine is retrofitted, despite B21 being the newer/more elaborated file.
- **B21 & C21** both order their two derivation-route subsubsections as
  *Analytical* (B21 line 200 / C21 line 211) **then** *Probabilistic* (B21 line 287
  / C21 line 280) — the reverse of C2's canonical *Probabilistic-then-Analytical*
  order (`09_model_c2.tex:88` / `:135`). The reversal is internally consistent
  between B21 and C21 (looks like a deliberate convention for the head-of-line
  pair), but diverges from the template.
- **B21**'s own "Limits and sanity checks" (line 305) recovers Model A but makes no
  explicit connection back to B2 (the `k`-ladder relationship is only discussed in
  `chapters/14_conclusion.tex:28`, as future work).

### B.4 Equation labels, macros, known cleanup items (CLAUDE.md §5)
- All `eq:` labels in B, X, B2, B21, C2, C21, comparison, results conform to
  `eq:<Model>:*` / `eq:gen:*` / `eq:comp:*` / `eq:results:*`.
- Preliminaries (`03_preliminaries.tex`) uses topical namespaces `eq:mm1:*`,
  `eq:erlangA:*`, `eq:kummer:*`, `eq:ode:*`, `eq:pk:*` — these are **not** "legacy
  mixed prefixes" in the sense CLAUDE.md warns about (no clashes, no ambiguity, used
  consistently as cross-references from C2/B2/etc.), but they sit outside the
  `eq:<Model>:*`/`eq:gen:*` enumeration in CLAUDE.md §5. Flagged for author
  sign-off only — recommend leaving as-is (forcing them into `eq:gen:*` would lose
  the topical grouping and is unlikely to be what "general lemma" was meant to
  cover).
- `\mathcal{I}` clash: **fully resolved**, zero remaining occurrences anywhere.
- Editorial scaffolding (`[NEW]`, `[MINE]`, `[Discuss:`, `[Introduce:`): **all
  resolved** — the two `[Discuss:]`/one `[Introduce:]` comments in
  `13_results.tex` recorded in `docs/REVIEW_CHECKLIST.md` are gone (verified by
  grep across the whole repo).
- Section titles: all model sections use "Analysis of Model-...", no redundant
  trailing "M/M/1", no "Variant" in any heading (the word "variant" appears only in
  body prose describing the head-of-line models, which is fine).

### B.5 Citations (CLAUDE.md §7)
- 16 distinct `\cite{}` keys used, all resolve in `references.bib`.
- 2 bib entries still **defined but never cited**: `abate2000asymptotic`,
  `tian2006vacation` — same residual flagged in the G1 audit. F1 (literature
  review) and F2 (conclusion) are now both done and neither entry got used; worth a
  final decision.
- No new uncited non-trivial claims found in B, X, B2, B21, C2, C21, comparison,
  results, or conclusion.

### B.6 Prose, grammar, British English
- No US spellings found in a repo-wide grep (`-ize`, `behavior`, `center`,
  `modeling`, etc., excluding the documented LaTeX-command exemptions).
- New content in `12_comparison.tex`, `13_results.tex`, `14_conclusion.tex` was
  spot-checked during the structural audit — no grammar/register issues raised.

### B.7 Figures, tables, code-pipeline consistency
- All 33 `fig:` labels and all `tab:` labels defined in `chapters/*.tex` are
  referenced via `Figure~\ref{...}` / `Table~\ref{...}` somewhere in prose. No
  defined-but-unreferenced floats.
- **New hardcoded numbers** in `chapters/12_comparison.tex` that duplicate the
  loss-fraction macro family from `results/derived_metrics.tex`:
  - `12_comparison.tex:600` — literal `$23.2\%$` restated immediately beside
    `\lossCtwoSeventy` (=0.232) in the same sentence.
  - `12_comparison.tex:601` — `$L=0.099$` (system-wide loss at ρ=0.70) has no
    `\L*`-style macro — same gap as the known Table 9 (`tab:comp:loss`) system-wide
    `L` column residual from the G1 audit, now also appearing in prose.
  - `12_comparison.tex:629-630` — `$L_1=0.044$` / `$L_1=0.468$` (off-grid
    θ1=0.05/5.0 sweep endpoints at ρ=0.70), same formula family, no macro.
- `Code/` was not modified since the last audit (confirmed via `git diff --stat`),
  so no pipeline-staleness check was needed this pass.

### Still open from prior audits (carried forward unchanged)
1. `main.tex:59` — `TODO-confirm` working title (1 of 3 candidates drafted in the
   comment).
2. `chapters/06_model_a.tex:52` — `[AUTHOR]` flag on the Probabilistic/Analytical
   subsubsection ordering vs. the canonical spine.
3. `chapters/06_model_a.tex:121` — `TODO: cite` — Kernel Method reference (e.g.
   Cohen 1982 or Bayer & Boxma).
4. `chapters/07_model_b.tex:479` — `TODO(citation)` — Hadamard finite-part
   reference (e.g. Hadamard 1923, *Lectures on Cauchy's Problem*).

### Resolved since last audit (not yet recorded in `docs/REVIEW_CHECKLIST.md`)
- `chapters/07_model_b.tex` (formerly ~line 422) — `[AUTHOR]` flag on the kernel
  regularisation of the 0·∞ limit — now resolved into narrative prose (lines
  ~474-511, "Hadamard finite part" discussion).
- `chapters/05_model_x.tex` (formerly ~line 420) — `[AUTHOR]` flag on the
  Model-B-vs-X closure contrast — now resolved into narrative prose (lines
  ~563-594).
- `chapters/13_results.tex` — the two `[Discuss: ...]` and one `[Introduce: ...]`
  scaffolding comments — removed/resolved.

---

## C. Claude-Code Task List (ordered)

| # | Task | Files | Rationale |
|---|---|---|---|
| C1 | Refresh `docs/REVIEW_CHECKLIST.md` / `docs/REPO_MAP.md` baseline: new build stats (105pp, 20/4 overfull/underfull), record the 3 resolved author items above, confirm the 4 still-open items. | `docs/REVIEW_CHECKLIST.md`, `docs/REPO_MAP.md` | Bookkeeping only; no math/prose changes. No dependencies — do first. |
| C2 | Add a forward cross-reference in `chapters/10_model_b21.tex`'s "Limits and sanity checks" (~line 305) pointing to the `k`-ladder discussion in `chapters/14_conclusion.tex:28`, making the B21↔B2 relationship explicit at the point a reader expects it. | `chapters/10_model_b21.tex` | Small prose addition, no structural decision needed, no dependencies. |
| C3 | Cosmetic overfull/underfull-box cleanup for the four worst offenders (61.97pt `13_results.tex:27-44`, 57.85pt `06_model_a.tex:208`, 43.37pt `09_model_c2.tex:265`, 41.57pt `09_model_c2.tex:74`) via rewording/line-break hints. Leave the <15pt boxes. | `chapters/13_results.tex`, `chapters/06_model_a.tex`, `chapters/09_model_c2.tex` | Cosmetic, independent of everything else. |
| C4 | Extend `Code/compute_derived_metrics.py` with (a) a system-wide loss-fraction macro family (`\L*`, `L = L1·ρ1/ρ`) and (b) macros for the off-grid θ1=0.05/5.0 sweep `L1` values; regenerate `results/derived_metrics.tex`; then replace the hardcoded numbers at `12_comparison.tex:600,601,629-630` and Table 9's system-wide `L` column with the new macros. | `Code/compute_derived_metrics.py`, `results/derived_metrics.tex`, `chapters/12_comparison.tex` | Closes the macro-coverage gap (B.7) and the known Table 9 residual in one pass. Mechanical but requires running the metrics script — moderate effort. Independent of author decisions. |
| C5 | **[Conditional on D-b]** Reorder `chapters/10_model_b21.tex` (~lines 200/287) and `chapters/11_model_c21.tex` (~lines 211/280) so "Probabilistic approach" precedes "Analytical approach", matching C2's order. Pure reorder + heading swap, no content change. | `chapters/10_model_b21.tex`, `chapters/11_model_c21.tex` | Only proceed if the author wants strict template conformance (D-b); otherwise document the reversed order as a deliberate head-of-line convention instead. |
| C6 | **[Conditional on D-a]** Retrofit explicit C2-style subsection headings into `chapters/08_model_b2.tex`'s Stage 2/3 proof content ("Determination of `P_y(y)`" with a kept "Probabilistic approach (not applicable)" heading, "Recovery of `P(x,y)`", "Limits and sanity checks"), bringing B2 in line with B21. | `chapters/08_model_b2.tex` | Only proceed if the author wants B/B2/X retrofitted with headings even where a route doesn't apply (D-a). |
| C7 | **[Conditional on D-c/D-d/D-e]** Once the author supplies: (i) the Kernel Method citation (06_model_a.tex:121), (ii) the Hadamard finite-part citation (07_model_b.tex:479), (iii) the bib decision for `abate2000asymptotic`/`tian2006vacation`, and (iv) the chosen working title — add the bib entries + `\cite{}`s, resolve/remove the bib entries, and update `\title{}` in `main.tex`, removing the corresponding `TODO`/`[AUTHOR]`/`TODO-confirm` comments. | `references.bib`, `chapters/06_model_a.tex`, `chapters/07_model_b.tex`, `main.tex` | Each sub-item is a 2-minute mechanical edit once the author input exists; bundle into one pass. |
| C8 | Re-run `latexmk -pdf main.tex`, confirm clean (0 errors/warnings), update `docs/REVIEW_CHECKLIST.md` with the resulting page count and box counts. | — (build only) | Final verification after C2–C7. |

## D. Author-Only Task List

- **D-a.** Decide whether Models B, X, and B2's embedded Stage 2/3 content should get
  explicit C2-style subsection headings (including "kept-but-explained" headings for
  routes that don't apply), or whether flat-prose synthesis is acceptable for these
  "open"/structurally-different models. → gates C6.
- **D-b.** Decide whether B21/C21's "Analytical-then-Probabilistic" order is a
  deliberate convention for the head-of-line experiment pair (document it as such)
  or should be reordered to match C2's "Probabilistic-then-Analytical". → gates C5.
- **D-c.** Decide the fate of the two unused bib entries `abate2000asymptotic` and
  `tian2006vacation` — remove, or identify where they should be cited.
- **D-d.** Confirm the working title (`main.tex:59`, 1 of 3 drafted candidates).
- **D-e.** Supply sources for the two open citation TODOs — Kernel Method
  (`06_model_a.tex:121`, e.g. Cohen 1982 / Bayer & Boxma) and Hadamard finite-part
  (`07_model_b.tex:479`, e.g. Hadamard 1923). Claude cannot invent these keys.
- **D-f.** (Optional, low priority) Confirm whether the preliminaries' topical
  equation-label namespaces (`eq:mm1:*`, `eq:erlangA:*`, `eq:kummer:*`, `eq:ode:*`,
  `eq:pk:*`) should remain as-is (recommended) or be migrated under `eq:gen:*` per
  CLAUDE.md §5's literal enumeration.

## E. Recommended Execution Order

- **Phase 1 — independent, no dependencies (run anytime, any order):**
  C1 (baseline refresh) → C2 (B21 cross-ref) → C3 (overfull-box cleanup) → C4
  (derived-metrics macro extension). These touch disjoint concerns and can be
  committed separately.
- **Phase 2 — author decisions (can happen in parallel with Phase 1, do not block
  it):** D-a, D-b, D-c, D-d, D-e, D-f.
- **Phase 3 — gated by Phase 2:**
  - If D-a → yes: C6 (B2 heading retrofit).
  - If D-b → reorder: C5 (B21/C21 reorder).
  - Once D-c/D-d/D-e have answers: C7 (citations, bib cleanup, title).
- **Phase 4 — final:** C8 (rebuild + refresh checklist), after all of Phase 1 and
  any executed Phase-3 tasks land.

D-f has no blocking effect on any Claude task (it's a documentation-only
clarification) and can be resolved whenever convenient.
