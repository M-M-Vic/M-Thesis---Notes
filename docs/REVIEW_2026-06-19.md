# Thesis Review & Action Plan — 2026-06-19

> Full whole-thesis audit (read-only diagnostic). Rubric: `CLAUDE.md`. Prior audits:
> `docs/REVIEW_2026-06-12.md`, `docs/REVIEW_CHECKLIST.md` (both now **stale** — see C1).
> Entry point: `main.tex`. Reviewed every chapter, the generated tables/macros, the
> bibliography, and a clean rebuild.

---

## A. Executive Summary

The thesis is in strong shape: it **compiles cleanly** (0 errors, 0 LaTeX warnings, all
273 labels unique, all 630 `\ref`/`\eqref`/`\Cref` targets resolve), at **98 pages**
(down from the 105 pp baseline — expected, from the Model-X analysis deletion + the
Results/Comparison consolidation). The two biggest restructures since the last audit —
**(i)** Model-X's analysis section demoted to `rem:B:modelX` inside Model-B, and **(ii)**
`12_comparison.tex` now nested inside `13_results.tex` with the exhaustive plots moved to
the new `15_appendix_numerics.tex` — both landed cleanly with **no dangling references**.
The CLAUDE.md `\mathcal{I}` notation clash is now **fully resolved**. Remaining issues are
all minor: a handful of label/cross-reference inconsistencies, one orphaned table, a
loss-fraction symbol used two ways, and the abstract drifting from body notation. No
mathematical or structural defects were found.

**Headline risks:** none blocking. The highest-value cleanups are notation unification
(loss fraction `L_1` vs `P_{\mathrm{loss}}^{(1)}`), one unlabelled theorem + the
"remark following Lemma 1" that isn't actually a `remark` environment, and bringing the
repo docs back in sync with the current file structure.

---

## B. Findings by Category

### B.1 Build cleanliness (CLAUDE.md §5 build loop)

- **PASS.** `latexmk -pdf main.tex` → exit 0, `main.pdf` = **98 pages**.
- **0 errors, 0 LaTeX/hyperref/package warnings.** No undefined refs, no undefined
  citations, **no duplicate labels**, no literal `??`.
- Boxes: **16 overfull + 4 underfull hbox** (0 vbox). All cosmetic, no regression. Worst
  offenders (unchanged from baseline):
  | too wide | file:line |
  |---|---|
  | 36.86 pt | `chapters/02_literature.tex:51-69` |
  | 14.85 pt | `chapters/06_model_a.tex:240` |
  | 14.85 pt | `chapters/06_model_a.tex:311` |
  | 13.48 pt | `chapters/08_model_b2.tex:297-304` |
  | 13.48 pt | `chapters/09_model_c2.tex:273` |

  Remaining 15 boxes are < 8 pt.
- **3** cosmetic pdfTeX `destination with the same identifier` warnings (down from 10) —
  harmless hyperref duplicate-anchor notes.

### B.2 Notation & conventions (CLAUDE.md §1–§3)

**Conforming (verified):**
- `π₀` vs `π(0,0)` kept distinct in every section (C₂ even restates the convention at
  `09_model_c2.tex:289`).
- `N1, N2, N` consistently described as in-queue / in-service excluded.
- Every principle named on use: Kernel Method, PASTA, Pollaczek–Khinchine, memorylessness,
  Little's Law, Vieta, Pincherle, renewal–reward, Poisson superposition, ergodic theorem.
- **`\mathcal{I}` clash RESOLVED** (CLAUDE.md §5 known cleanup item): `\mathcal{I}` no
  longer appears anywhere; auxiliary integrals are `\mathcal{J}`/`\mathcal{H}`, the
  idle/busy indicator is now `\mathbf{1}{...}`.

**Findings:**
- **N1 — Loss-fraction symbol used two ways.** The class-1 loss fraction is defined as
  `L_1` in `chapters/12_comparison.tex:362` (`eq:comp:lossfrac`) and called `L_1` again at
  `chapters/15_appendix_numerics.tex:322`, but is written `P_{\mathrm{loss}}^{(1)}` in the
  loss-accounting table `chapters/15_appendix_numerics.tex:279,281,286` (`tab:comp:loss_acct`)
  and in `chapters/14_conclusion.tex:23`. One quantity, two symbols — violates the §1
  consistency mandate.
- **N2 — Abstract notation drifts from body.** `main.tex:75,77` writes the head-of-line
  models as literal `$B_2^H$` / `$C_2^H$` (italic *H*) instead of the `\BH` / `\CH` macros
  (upright `\mathrm{H}`) used throughout the body — visual inconsistency.
- **N3 — "head-of-the-line" vs "head-of-line".** Abstract (`main.tex:75,77`) uses
  "head-of-the-line" (×2); the entire body uses "head-of-line".

### B.3 Section-structure conformance (CLAUDE.md §6)

All model sections follow the canonical spine (C₂ is the template; A, B, B₂, B₂¹, C₂¹
conform with model-appropriate adaptations). Model-B's openness is correctly framed
(reduces to a first-kind Volterra equation), and Model-X is demoted to `rem:B:modelX`.
Head-of-line sections correctly state their own fundamental-equation lemmas (they cannot
be obtained by zeroing parameters in `lem:gen:fundamental_equation`).

**Findings:**
- **S3 — Stability-statement placement non-uniform.** Models A, B, B₂, B₂¹, C₂¹ carry a
  standalone `\textbf{Stability.}` paragraph; C₂ embeds stability inside the theorem
  statement (`09_model_c2.tex:17`). All are explicit (so §2 is satisfied), but placement
  differs.

**Still open from prior audits (carried forward, not regressions):**
- **[AUTHOR] Probabilistic/Analytical ordering, Model-A** — `chapters/06_model_a.tex:52`.
  Model-A presents Analytical (`sssec:A:analytical`) before Probabilistic
  (`sssec:A:probabilistic`), reversed vs B₂/C₂. Flag still present; author's call.
- **S̃ state-space policy half-applied** (CLAUDE.md §6 author decision). `\widetilde{S}`
  is defined generally in `04_model_description.tex` (balance eqs + `lem:gen:PPGF_dynamics`)
  but *used* only in Model-A (`ssec:A:Stilde`). Author must choose: present once in
  Preliminaries, or extend to every model.

### B.4 Equation labels, macros, cleanup items (CLAUDE.md §5)

The `eq:<Model>:*` scheme is used throughout (`eq:A`, `eq:B`, `eq:B2`, `eq:C2`, `eq:B21`,
`eq:C21`, `eq:gen`, plus `eq:comp`, `eq:erlangA`, `eq:kummer`, `eq:pk`, `eq:mm1`). No
legacy mixed prefixes survive. Scaffolding tags: only the tracked `[AUTHOR]` at
`06_model_a.tex:52`. Section titles uniform ("Analysis of Model-…", no trailing "M/M/1").

**Findings:**
- **L1 — C₂'s main theorem is unlabelled.** `chapters/09_model_c2.tex:16` has no `\label`,
  whereas Model-A (`thm:A:PGF`), B₂ (`thm:B2`), B₂¹ (`thm:B21:PGF`), C₂¹ (`thm:C21:PGF`)
  are all labelled. Consequently the validation table (`13_results.tex:50,52,53`)
  references the A/B₂/C₂ theorems by **hardcoded prose** ("Theorem (Model-$A$)") while the
  head-of-line rows use `\ref{thm:B21:PGF}` / `\ref{thm:C21:PGF}`.
- **L2 — Theorem-label scheme uneven.** `thm:A:PGF`, `thm:B21:PGF`, `thm:C21:PGF` carry a
  `:PGF` suffix but `thm:B2` (`08_model_b2.tex:34`) does not.
- **L3 — "remark following Lemma 1" is not a `remark`.** Referenced 4× — B₂
  (`08_model_b2.tex:203`), C₂ (`09_model_c2.tex:137`), C₂¹ (`11_model_c21.tex:119`),
  conclusion (`14_conclusion.tex:7`) — but the target is a `\paragraph{Reading the
  fundamental equation.}` at `04_model_description.tex:272`, **not** a `\begin{remark}`
  environment (which the preamble defines and the thesis uses elsewhere:
  `rem:B:modelX`, `rem:B2:singular_limit`). It cannot be `\ref`'d, so the references read
  "the remark following Lemma~\ref{lem:gen:fundamental_equation}" descriptively.
- **L4 — One off-scheme equation label.** `eq:results:EN_A` (`12_comparison.tex:260`) is
  the lone `eq:results:` prefix among `eq:comp:*` labels in the comparison file.
- **L5 — Hardcoded result number.** `chapters/15_appendix_numerics.tex:27` writes
  "Lemma~2" instead of `Lemma~\ref{lem:gen:PPGF_dynamics}` (violates the
  reference-by-`\label` convention; cf. `project_model_x_reorder` lesson).

### B.5 Citations (CLAUDE.md §7)

- 18 keys defined, **16 cited, all resolve**; no invented keys.
- **Defined but never cited (carry-forward):** `abate2000asymptotic`, `tian2006vacation`
  (same two flagged at G1 and 2026-06-12; left in place by the author).
- **`% TODO: cite` flags (carry-forward author items):**
  - `chapters/06_model_a.tex:121` — Kernel Method reference (e.g. Bayer & Boxma, or Cohen).
  - `chapters/07_model_b.tex:483` — Hadamard finite-part integrals reference.

### B.6 Prose, grammar, British English (CLAUDE.md §5 spelling)

- **US spellings: clean.** Only matches are LaTeX-command exemptions (`\begin{center}`).
- Wording: "head-of-the-line" / "head-of-line" split (see N3).
- The longest-overfull paragraph (`02_literature.tex:51-69`, 36.86 pt) is the only
  >15 pt box and would benefit from a rewrap.

### B.7 Figures, tables, code pipeline (CLAUDE.md §8)

- **F1 — One orphaned table.** `tab:comp:loss_acct` (`15_appendix_numerics.tex:283`) is
  defined and displayed but never `Table~\ref`'d in prose (its intro at lines 269–274 names
  it without a cross-reference). All other 44 figure/table labels are referenced.
- **Derived-metrics macros: all 93 used, 0 unused.** Loss fractions, throughputs,
  premiums, waiting times etc. route through `results/derived_metrics.tex` (D1 pipeline).
- **F2 — Inline hardcoded CTMC readouts (drift risk).** Many numeric values are typed
  directly in prose and duplicate generated-table cells, e.g. `12_comparison.tex:315-318,333`
  (`π₀=0.3696`, `E[N]=0.8358`, `E[W₂]=3.6970`, `E[N₂]=7.5346`, …) and various lines in
  `15_appendix_numerics.tex`. If the notebooks regenerate with different parameters the
  prose desyncs silently. Some are genuinely illustrative — author's call whether to
  macro-ise (see D7).
- Pipeline markers (`%% BEGIN/END SIMULATION RESULTS`) intact; `Code/` not modified in
  scope, no staleness checked beyond table `\input`s (all four `tab_*.tex` resolve).

### B.8 Repo documentation (cross-cutting)

- **D-docs — `docs/REPO_MAP.md` and `docs/REVIEW_CHECKLIST.md` are stale.** They describe
  the *pre-restructure* layout: `main.tex` inputting `05_model_x.tex` (deleted) in
  A→B→X→B₂→C₂ order, `12_comparison.tex` as a top-level `\input` (now nested in
  `13_results.tex:73`), no `15_appendix_numerics.tex`, and a 105 pp baseline (now 98 pp).
  The file→section map and build baseline both need regenerating.

---

## C. Claude-Code Task List (ordered, autonomous)

| # | Title | Files | Rationale |
|---|---|---|---|
| **C1** | Refresh repo docs to current structure | `docs/REPO_MAP.md`, `docs/REVIEW_CHECKLIST.md` | Remove `05_model_x`, record `12`→nested-in-`13`, add `15_appendix_numerics`, update baseline to 98 pp + new file→section map. No thesis edits. |
| **C2** | Fix hardcoded "Lemma~2" | `15_appendix_numerics.tex:27` | Replace with `Lemma~\ref{lem:gen:PPGF_dynamics}` (reference-by-label convention). |
| **C3** | Label C₂ theorem + unify validation-table refs | `09_model_c2.tex:16`, `13_results.tex:50,52,53` | Add `\label{thm:C2:PGF}`; switch T-A/T-B₂/T-C₂ rows to `\ref` the theorems (A/B₂ already labelled). Optionally rename `thm:B2`→`thm:B2:PGF` (+1 ref) for scheme uniformity (L2). |
| **C4** | Promote "Reading the fundamental equation" to a `remark` | `04_model_description.tex:272`; refs in `08_model_b2.tex:203`, `09_model_c2.tex:137`, `11_model_c21.tex:119`, `14_conclusion.tex:7` | Wrap as `\begin{remark}\label{rem:gen:reading}…\end{remark}`; change the 4 "remark following Lemma 1" mentions to `\ref{rem:gen:reading}`. Makes a 4×-cited object `\ref`-able and matches existing remark usage (L3). |
| **C5** | Unify loss-fraction notation to `L_1` | `15_appendix_numerics.tex:279,281,286`, `14_conclusion.tex:23` | Replace `P_{\mathrm{loss}}^{(1)}` with `L_1` (matches `eq:comp:lossfrac`). (N1) |
| **C6** | Fix abstract head-of-line notation/wording | `main.tex:75,77` | `$B_2^H$`→`$\BH$`, `$C_2^H$`→`$\CH$`; "head-of-the-line"→"head-of-line" (N2, N3). |
| **C7** | Cross-reference the orphaned loss-accounting table | `15_appendix_numerics.tex:269-274` | Add a `Table~\ref{tab:comp:loss_acct}` sentence in its intro prose (F1). |
| **C8** | Rename off-scheme equation label | `12_comparison.tex:260` (+ any refs) | `eq:results:EN_A`→`eq:comp:EN_A` for namespace consistency (L4). |
| **C9** | (Optional, formatting-only) box + whitespace tidy | `02_literature.tex:51-69`, `09_model_c2.tex:521-535` | Rewrap the 36.86 pt paragraph; drop ~17 trailing blank lines in C₂. No content change. |

C1, C2, C5, C6, C7, C8, C9 are mutually independent. C3 and C4 add/rename labels and
update references — do them before any later task that would add new cross-refs.

## D. Author-Only Task List (judgment required)

| # | Item | Locus |
|---|---|---|
| **D1** | Confirm the working title (3 candidates) | `main.tex:59` `TODO-confirm` |
| **D2** | Supply Kernel Method citation | `06_model_a.tex:121` `TODO: cite` |
| **D3** | Supply Hadamard finite-part citation | `07_model_b.tex:483` `TODO(citation)` |
| **D4** | Decide Model-A Probabilistic/Analytical ordering (swap to match B₂/C₂, or keep + document) | `06_model_a.tex:52` `[AUTHOR]` |
| **D5** | Decide S̃ state-space policy (present once in Preliminaries, or extend to every model) | CLAUDE.md §6; currently `04` + Model-A only |
| **D6** | Cite or remove the two unused bib entries | `references.bib`: `abate2000asymptotic`, `tian2006vacation` |
| **D7** | Decide whether to macro-ise inline CTMC sweep numbers (drift risk) or keep as illustrative | `12_comparison.tex`, `15_appendix_numerics.tex` (F2) |

## E. Recommended Execution Order

- **Phase 1 — independent cleanups (no deps, clean build preserved):** C1, C2, C5, C6,
  C7, C8, then C9 if desired. Pure label/notation/doc/formatting edits; recompile to
  confirm 0 warnings.
- **Phase 2 — label/structure normalisation:** C3 then C4. These add `\label`s and a
  `remark` environment and rewire cross-references; land them before any future work that
  introduces new `\ref`s to those targets. Recompile; confirm refs resolve.
- **Phase 3 — author decisions:** D1–D7. **Blocking relationships:** D4 (ordering) and D5
  (S̃ policy) are the only ones that could trigger further Claude-Code structure edits;
  D2/D3/D6 are citation insertions/removals that no Claude task depends on. None of
  Phase 1/2 depends on Phase 3.
- **Phase 4 — re-audit:** rebuild from clean, re-run `/thesis-review` (or the
  label/ref/citation greps) to confirm the orphan table, the loss-fraction symbol, the
  unlabelled theorem, and the doc staleness are all closed.

---

*No files were edited in this pass. This report is the sole artefact.*
