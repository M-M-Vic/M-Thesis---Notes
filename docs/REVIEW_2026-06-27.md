# Whole-Thesis Review — 2026-06-27

> Full audit (`/thesis-review`, no `since` argument). Entry point `main.tex`.
> Rubric: `CLAUDE.md` (binding). Prior audits: `docs/REVIEW_2026-06-12.md`,
> `docs/REVIEW_2026-06-19.md`, `docs/REVIEW_CHECKLIST.md`. This pass diagnoses; the
> autonomous fixes it recommends (C-tasks) were executed in the same session at the
> author's request — see the closing note.

## A. Executive Summary

The thesis is in strong shape. The build is **clean** (0 errors, 0 LaTeX/hyperref
warnings, 0 undefined refs/citations, no duplicate labels). Citations are a perfect
16-of-16 with no orphans. The structure conforms to the canonical spine in every model
section, and three previously-tracked author flags have since been resolved in source
(Model-A Probabilistic/Analytical ordering, the working-title `TODO-confirm`, and the
Kernel-Method `TODO: cite`). The only **correctness** finding is a localised
stability-strictness slip (`\rho\le1` where the model's own definition says `\rho<1`,
three occurrences). Everything else is cosmetic: four prose typos, one bare cross-reference,
and a batch of stale facts in `docs/REPO_MAP.md` / `docs/REVIEW_CHECKLIST.md` (page count,
the now-inlined comparison, the excluded appendix).

**Headline numbers.** Build PASS; **67 pages** (was 98 at the 2026-06-19 baseline — the
appendix is now excluded from the compiled PDF and the per-model mean-queue/waiting
subsections were archived out). 244 unique labels (chapters + `main.tex`), all unique;
518 `\ref`/`\eqref`/`\Cref` targets, all resolve. 10 overfull + 7 underfull hboxes (all
cosmetic). 1 cosmetic pdfTeX duplicate-anchor warning.

## B. Findings by Category

### §1 Build cleanliness
- **PASS.** Exit 0. `Output written on main.pdf (67 pages)`.
- 0 errors; 0 LaTeX / hyperref / package warnings; 0 undefined refs/citations; no literal `??`.
- **Page count 67** vs. the 98-page baseline recorded in `docs/REPO_MAP.md` §3. The drop is
  expected and accounted for: appendix `15_appendix_numerics.tex` is commented out at
  `main.tex:93` (2026-06-27), and the per-model mean-queue/waiting material was archived to
  `mean_queue_and_waiting_times.tex`. **`docs/REPO_MAP.md` is stale on this and should be
  refreshed** (see C4).
- **Boxes:** 10 overfull, 7 underfull `\hbox`; 0 vbox. Worst offenders:
  | too wide | location |
  |---|---|
  | 36.86 pt | `chapters/02_literature.tex:51–69` (carry-forward, content-touching — left per house rule) |
  | 14.85 pt | `chapters/06_model_a.tex:200` |
  | 14.85 pt | `chapters/06_model_a.tex:270` |
  | 13.48 pt | `chapters/08_model_b2.tex:181` |
  | 11.15 pt | `chapters/09_model_c2.tex:203` |
  The remaining boxes are < 5 pt. None block; all are cosmetic.
- **1 pdfTeX warning** (cosmetic): `destination with the same identifier (name{equation.3.6})`
  — a duplicate hyperref anchor on equation (3.6), harmless (down from 3 at the last baseline).

### §2 Notation & conventions (CLAUDE.md §1–§3)
- `\pi_0` vs `\pi(0,0)` kept distinct everywhere; the baseline identities `\pi_0=1-\rho`,
  `\pi(0,0)=\rho(1-\rho)`, `P(1,1)=1-\pi_0`, and `P(z,z)=\pi(0,0)/(1-\rho z)` are stated
  correctly (e.g. `06_model_a.tex:71–78`, `13_results.tex:145`). ✓
- `N_1,N_2,N` consistently described as in-queue, excluding the in-service customer. ✓
- `\widetilde{S}` appears only in Model-A (`06_model_a.tex`, `ssec:A:Stilde`) and the general
  Model-X description (`04_model_description.tex`), per the declared policy. No other section
  silently introduces it. ✓
- PGF/PPGF macros (`\tP`, `\tpi`, `P_x`, `P_y`, LST `\widetilde{B}_C`) used consistently. ✓
- Principles named explicitly throughout (Kernel Method, PASTA, Pollaczek–Khinchine,
  memorylessness, Little's Law). ✓
- **FINDING (correctness) — stability strictness.** Three restatements use the non-strict
  `\rho\le1`, contradicting each model's own stability *definition* (`\rho<1`) and CLAUDE.md
  §2 (a non-abandonment M/M/1-type chain is positive recurrent **iff** `\rho<1`; at `\rho=1`
  it is null-recurrent and no stationary law exists):
  - `chapters/06_model_a.tex:55` — Theorem~\ref{thm:A:PGF} hypothesis `$\rho\leq 1$`
    (cf. the definition `eq:A:stability` at `:22`, which says `<`).
  - `chapters/06_model_a.tex:72` — Corollary~\ref{cor:A:pi0} hypothesis `$\rho \leq 1$`.
  - `chapters/08_model_b2.tex:161` — `Under stability (i.e. $\rho\leq1$)` (cf. B₂'s own
    `$\rho<1$` at `:5` and `:33`).
  Pure internal-consistency fix; no judgement. → **C1**.

### §3 Section-structure conformance (CLAUDE.md §6)
- Canonical spine satisfied in every model section: Preamble → Stability → Reduced
  fundamental equation → Theorem (+ Corollary pulling out `\pi_0`, `\pi(0,0)`) →
  Determination of `P_y(y)` (Analytical then Probabilistic) → Recovery of `P(x,y)` →
  Limits & sanity checks. Verified for A, B₂, C₂, B₂ᴴ, C₂ᴴ; Model-B is the deliberate open
  case (Corollary + `clm:B:volterra` + `rem:B:modelX`). ✓
- **Resolved since prior audits:** the `[AUTHOR]` flag on Model-A's Probabilistic/Analytical
  ordering (the skill text still points to `06_model_a.tex:52`) is **gone**; the section now
  runs Analytical (`sssec:A:analytical`, `:114`) then Probabilistic (`sssec:A:probabilistic`,
  `:207`), which matches CLAUDE.md §6 step 5. No carry-forward needed. ✓
- Head-of-line sections (B₂ᴴ/C₂ᴴ) carry only the Analytical (Kernel-Method) route; this is
  appropriate (jockeying/abandonment-at-head leaves no probabilistic P-K identity, as the
  comparison §`sec:comparison` explains). ✓

### §4 Equation labels, macros, known cleanup items (CLAUDE.md §5)
- **Label scheme conformant.** Model labels live in `eq:A:*`, `eq:B:*`, `eq:B2:*`, `eq:C2:*`,
  `eq:gen:*`, plus the head-of-line `eq:B21:*` / `eq:C21:*` (established prefixes) and a clean
  `eq:comp:*`. Preliminaries use topical namespaces `eq:mm1:*`, `eq:erlangA:*`, `eq:pk:*`,
  `eq:kummer:*`, `eq:ode:*` — sensible and not a model-namespace violation. No legacy mixed
  prefixes. ✓
- **`\mathcal{I}` clash resolved.** The server idle/busy indicator `\mathcal{I}` no longer
  appears anywhere (the indicator is now written `\mathbf{1}_{\{n_1\ge1\}}`). The auxiliary
  integrals are unified on `\mathcal{H}_1,\mathcal{H}_2` for the partial `[0,x]` integrals in
  **both** B₂ (`08:57–59`) and C₂ (`09:167–168`); C₂ additionally carries `\mathcal{J}_0,
  \mathcal{J}_1` for its distinct full `[0,1]` boundedness integrals (`09:118–120`), with the
  bridging relation `\mathcal{H}_2(1,y)=\mathcal{J}_1(y)` stated explicitly at `09:170`. The
  CLAUDE.md §5 cleanup item is effectively closed; a fuller unification (folding C₂'s
  `\mathcal{J}` into the `\mathcal{H}` family) is optional and is an author call. ✓ (minor)
- **No editorial scaffolding** (`[NEW]`, `[MINE]`, `[Discuss:]`, `[Introduce:]`, `[AUTHOR]`)
  survives in `chapters/` or `main.tex`. ✓
- **Section titles** uniformly "Analysis of Model-…"; no redundant trailing "M/M/1". ✓

### §5 Citations (CLAUDE.md §7)
- **16 cited keys, 16 defined, perfect 1:1.** No defined-but-uncited entries (the previously
  flagged `abate2000asymptotic` / `tian2006vacation` have been removed from
  `references.bib`). No cited-but-undefined keys. No invented keys. ✓
- **Resolved:** the Kernel-Method `TODO: cite` (old `06_model_a.tex:121`) is gone; the method
  now cites `\cite{cohen1956delay}` (`06:122`). ✓
- **Author item (carried, now un-flagged in source):** the finite-part / regularisation
  passage at `chapters/07_model_b.tex:375` invokes the Hadamard finite-part concept without a
  citation. It is framed as the analytical obstruction that leaves Model-B open (not a result
  being *used*), so it is defensible as-is; if a citation is wanted, the author must supply
  the source (cannot be invented). → **D1**.

### §6 Prose, grammar, British English
- **British spelling clean.** No US variants (`-ize`, `behavior`, `center`, `modeling`, …)
  outside the documented LaTeX-command exemptions. ✓
- **Typos (8 fixes)** → **C2**:
  - `chapters/06_model_a.tex:122` — "At **anysuch** root" → "any such".
  - `chapters/10_model_b21.tex:6` — "This **signifficantly** simplifies" → "significantly".
  - `chapters/10_model_b21.tex:4` — "**theis** model" → "this".
  - `chapters/09_model_c2.tex:144` — "analytically **through the through the** Beta-integral"
    (doubled "through the").
  - `chapters/09_model_c2.tex:147` — "**ans** substitute" → "and"; "for **brievity**" → "brevity".
  - `chapters/08_model_b2.tex:80` — "obtaining **the the** standard form" (doubled "the").
  - `chapters/08_model_b2.tex:78`, `:69` — "**As in for** Model-$B$" → "As in Model-$B$"
    (two occurrences: doubled preposition).
- **Bare cross-reference** → **C3**: `chapters/06_model_a.tex:7` — "an evaluation is provided
  in `\ref{apx:A:PPGF_Stilde}`" prints a bare number; should read "in
  Approximation~\ref{…}" (the target is an `approximation` environment, referred to elsewhere
  as "Approximation~\ref{apx:A:PPGF_Stilde}", e.g. `13_results.tex:42`).

### §7 Figures, tables, code pipeline
- **All 31 figure labels and all 10 table labels are cross-referenced** in prose — no orphans
  (verified by `\label` vs `\ref`/`\Cref` set comparison). ✓
- **Numbers are macro-driven.** `13_results.tex` routes every derived quantity through the
  `results/derived_metrics.tex` macros (`\piZeroCtwoSeventy`, `\ENASeventy`, `\EWtwoBtwoSeventy`,
  …); the only inline literals are CTMC truncation-tail magnitudes (`6\times10^{-4}` etc.),
  which are descriptive, not derived metrics. No new hardcoded values to route. ✓
- **Appendix excluded (observation, not a defect).** `15_appendix_numerics.tex` is commented
  out at `main.tex:93`; its figures/tables (`tab:comp:EN_sweep`, `tab:prio:benefit`,
  `tab:conv:rates`, `tab:comp:loss_acct`, `fig:val_*`, `fig:conv:*`) and their cross-refs are
  internally self-consistent, and no *compiled* chapter references an appendix-only label
  (hence the clean build). If the appendix is later re-enabled, re-run the cross-reference and
  box checks. The generated `figures/results/*` and `figures/validation/*` plots that only the
  appendix consumed are currently "dark" (not in the PDF) — author's deliberate scope choice.

### Still open from prior audits (carried forward)
1. **Hadamard finite-part citation** — `chapters/07_model_b.tex:375` (was `:483`/`:391`).
   Author must supply the source. (= **D1**)
2. **S̃ state-space policy** — half-applied by design: general derivation in
   `04_model_description.tex`, used only in Model-A. CLAUDE.md §6 records this as the resolved
   policy (S̃ retained only in Model-A; the recursion is a documented negative result). No
   action; noted for completeness.

**Cleared since last audit** (no longer open): working-title `TODO-confirm` (resolved — the
title at `main.tex:59` is committed); Kernel-Method citation (resolved, `06:122`); Model-A
`[AUTHOR]` ordering flag (resolved); `abate2000asymptotic` / `tian2006vacation` orphan bib
entries (removed).

## C. Claude-Code Task List (ordered, autonomous)

1. **C1 — Fix stability strictness.** Files: `chapters/06_model_a.tex` (`:55`, `:72`),
   `chapters/08_model_b2.tex` (`:161`). Replace `\rho\le1` with `\rho<1` to match each
   model's own definition and CLAUDE.md §2. *(Correctness; do first.)*
2. **C2 — Fix eight prose typos.** Files: `chapters/06_model_a.tex:122`,
   `chapters/10_model_b21.tex` (`:4`, `:6`), `chapters/09_model_c2.tex` (`:144`, `:147`),
   `chapters/08_model_b2.tex` (`:69`, `:78`, `:80`). *(Cosmetic; independent of C1.)*
3. **C3 — Fix bare cross-reference.** File: `chapters/06_model_a.tex:7`. Prefix the `\ref`
   with "Approximation~". *(Cosmetic cross-ref style.)*
4. **C4 — Refresh `docs/REPO_MAP.md`.** Update the build baseline (67 pp), the
   chapter table (no `12_comparison.tex`; comparison is now a subsection inside
   `13_results.tex`), the appendix-excluded note, and the references count (16 entries, all
   cited). *(Documentation; depends on a clean post-C1–C3 build for the page count.)*
5. **C5 — Append a 2026-06-27 entry to `docs/REVIEW_CHECKLIST.md`** recording this audit and
   the C1–C4 fixes, for continuity of the audit trail. *(Documentation.)*

All five are independent except C4 (read the final page count after C1–C3 compile) and C5
(records the others). Rebuild with `latexmk -pdf main.tex` after C1–C3 and confirm 0
errors / 0 warnings.

## D. Author-Only Task List

1. **D1 — Hadamard finite-part citation** (`07_model_b.tex:375`). Supply a source for the
   finite-part/regularisation reading, or leave it as the framed open obstruction. Cannot be
   invented.
2. **D2 (optional) — Kernel-Method attribution** (`06_model_a.tex:122`). The method currently
   cites `cohen1956delay`; the canonical Kernel-Method reference `fayolle1999random` (already
   in `references.bib`) could be added or substituted. Author's call.
3. **D3 (optional) — Appendix scope.** Decide whether `15_appendix_numerics.tex` stays
   excluded. If re-enabled, its (currently dark) generated figures/tables return to the PDF
   and the cross-ref/box checks should be re-run.
4. **D4 (optional, cosmetic) — 36.86 pt overfull box** at `02_literature.tex:51–69`. Touching
   it edits prose, so it is left for the author per the no-content-change house rule.

## E. Recommended Execution Order

- **Phase 1 — autonomous fixes (no author input):** C1 → C2 → C3, then `latexmk -pdf` and
  confirm clean. These have no interdependencies beyond compiling once at the end.
- **Phase 2 — documentation (depends on Phase 1 build):** C4 (uses the final 67-page count),
  then C5.
- **Phase 3 — author decisions (non-blocking):** D1 (citation sourcing) and D2/D3/D4
  (optional). None of these block any C-task; they can be triaged at leisure.

No author decision blocks any Phase-1/2 task.

---

*Closing note.* At the author's instruction, Phase 1 (C1–C3) and Phase 2 (C4–C5) were
executed in this same session immediately after this report was written; the post-fix build
was re-confirmed clean (0 errors / 0 warnings, 67 pages). D-items remain for the author.
